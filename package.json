{
  "name": "trackflow",
  "version": "1.0.0",
  "description": "Trackflow monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "consumer",
    "shared"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "corepack": "^0.21.0",
    "pnpm": "^8.15.4"
  }
}
